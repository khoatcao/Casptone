- name: Copy deployment configuration
  copy:
    src: "{{ ENVIRONMENT_NAME }}-service-deployment.yaml"
    dest: $HOME

- name: Apply deployment configuration
  shell: "./bin/kubectl apply -f {{ ENVIRONMENT_NAME }}-service-deployment.yaml"
  args:
    chdir: $HOME
